import React from 'react';
import {Link} from "react-router-dom";
const Router = () => (
    <div>
    <h1>Adi</h1>
        <Link to="/">
            <button>Назад</button>
        </Link>
    </div>
);

export default Router;